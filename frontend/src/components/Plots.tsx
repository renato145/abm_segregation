import React from "react";

interface Props {
  className?: string
}

export const Plots: React.FC<Props> = ({className}) => {
  return (
    <div className={className}>
      TODO: Plots
    </div>
  );
};